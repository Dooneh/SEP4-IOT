services:
  sep4-python-env:
    build:
      context: .
      dockerfile: Dockerfile
    image: sep4-java-env
    container_name: iot-container
    volumes:
      - ../:/app
    working_dir: /app
    command: bash
    stdin_open: true
    tty: true
